/**
 * @deprecated - This file is deprecated and exists only for backwards compatibility.
 * Import types directly from src/templates/_core/types.ts instead.
 */

// Re-export types from the new template system
export * from '../templates/_core/types';

// Legacy interfaces preserved for backwards compatibility
// These will be removed in a future update
